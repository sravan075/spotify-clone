export const reducerCases = {
    SET_TOKEN: "SET_TOKEN",                 // Action type for setting the token
    SET_PLAYLISTS: "SET_PLAYLISTS",         // Action type for setting playlists
    SET_USER: "SET_USER",                   // Action type for setting user info
    SET_PLAYLIST: "SET_PLAYLIST",           // Action type for setting a selected playlist
    SET_PLAYING: "SET_PLAYING",             // Action type for setting the currently playing track
    SET_PLAYER_STATE: "SET_PLAYER_STATE",   // Action type for setting the player state (playing/paused)
    SET_PLAYLIST_ID: "SET_PLAYLIST_ID",     // Action type for setting the selected playlist ID
};
